@*
	The badge on a stored row whose Home Assistant dropdown no longer offers its value, on both screens that
	map one. The words are HelperOrphan's, so the two cannot drift apart again — they had already reached
	"renamed in Home Assistant" on one screen and "removed from the helper" on the other, guessing differently
	at something neither can know.
*@

<span class="badge bad" title="@HelperOrphan.Explanation">@HelperOrphan.Badge</span>
