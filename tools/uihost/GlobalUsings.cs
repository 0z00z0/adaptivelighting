// The web view helpers moved out of AdaptiveLighting.Web.Services into their own namespace. Global rather than a
// razor @using, which resolves relative to the generated namespace and binds to the wrong thing.
global using AdaptiveLighting.Web.Presentation;
